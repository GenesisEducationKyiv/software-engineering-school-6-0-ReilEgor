apiVersion: v1
kind: Service
metadata:
  name: tracking
spec:
  selector:
    app: tracking
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: health
      port: 8081
      targetPort: 8081
  type: ClusterIP
