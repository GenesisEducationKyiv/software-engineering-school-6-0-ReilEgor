apiVersion: 1

providers:
  - name: "RepoNotifier"
    orgId: 1
    folder: "RepoNotifier"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
